# CloudBase environment ID (required)
VITE_CLOUDBASE_ENV_ID=your-cloudbase-env-id

# Invite code for each account role (required)
VITE_ME_INVITE_CODE=your-code-for-me
VITE_HER_INVITE_CODE=your-code-for-her

# Private wall password (recommended)
VITE_PRIVATE_WALL_PASSWORD=your-private-wall-password

# Optional: max chat media size in MB (default 300)
# VITE_MAX_CHAT_FILE_MB=300

# Optional: static hosting base path, default '/' (set to '/your-sub-path/' when needed)
# VITE_PUBLIC_BASE=/
